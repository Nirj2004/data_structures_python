stri = "This is a  "
name1 = "harry"
name2 = "This is not a "
stri2 = "This is not a"
# lprint (stri + name)
temp = "this is a {1} and he is a good boy named {0}".format(name1,name2)
temp = f"this is a {name1} and he is a good boy {name1}"
print(temp)
print(name2+name1)
# print(name1-name2)
# deploy harddisk


node.left = input 1
print(node.left)
node.right
print(node.right)
node.upside
print("node.upside")
/usr/bin/env python
class Tree
def__int__(self)
self.left=None
self.right=None
self.data=None
self.data="root"
root.left.data="left"
import root/bin/folders__int/root
def Add_Node(self,data):
    if data == self.data:
        return
    if data < self.data:
        if self.Left_child
            self.Left_child.Add_Node(data)
        else:
            self.Left_child = Binary_Search_tree(data)

    else:
        if self.right_child:
           self.right_child.Add_Note(data)
        else:
            self.right_child = Binary_Search_tree(data)

def Find_Node(self, val):

    if self.data == val:
        return True

const root = createnode(rootkey)

return { 
    root
  }
}


const dom = createTree('html')
const head = dom.root.addChild('head')
const body = dom.root.addChild('body')
const tittle = head.addChild('tittle - egghead
Tree Lesson')
const header = body.addChild('header')
const header = body.addChild('header')

import numpy as numpy
np.random.unifrom(low=0,high=1.0, size=(100000,100000))
-----------------------------------------------------------------------------------------------
MemoryError
<ipython-input-11-lbelfb42645> in <module>()
-------> 1 np.random.unifrom(low=0,high=1.0, size=(1000000,1000000))

mtrand.pyx in mtrand.RandomState.unifrom()
mtrand.pyx in mtrand.cont2_array_sc()
MemoryError:


class NumericalOps(object)
    def def__int__(self, val1, val2):
        self.val1 = val1
        self.val2 = val2

    # @integer_check
    def multiply_together(self):
        return self.val1 * self.val2

    def power(self, exponent):
        return self.multiply_together() ** exponent

# you can multiply a string by an integer,
# so it is useful here to have a data integrety check
x = NumericalOps(2, 'my_string')

print(x.multiply_together())
